# Noeis product realignment — Pass 3 acceptance

Date: 2026-08-10

## Verdict

**PASS — Pass 3 is complete locally with automated, rendered, and disposable-data persisted acceptance.**

The calm Think workspace now supports both blank thought and grounded continuation. A run-unique disposable corpus proved the real authenticated Wiki-to-Think route from a structurally accepted revision, exact identity continuity, cache-cold reload, source provenance, proposal dismissal, and accepted-Wiki immutability without response interception.

This is bound to a manifest of the dirty integration worktree, not an exact Git tree. It is not clean-candidate, merge, deploy, or production evidence.

## Standout delivered

- Think Home now places `Ask, think, or build…` directly beneath the return orientation instead of below the activity queue.
- Selected Concepts use the shared contextual Thought partner presentation: open on desktop and an explicit drawer on mobile.
- A fresh Concept keeps partner retrieval optional. “Run partner scan after create” is off by default, the name field receives focus, and no scan runs unless selected.
- Investigation orientation leads with the exact question and accepted relationship. Detailed evidence, tensions, and proposals remain separate in a closed disclosure.
- A Wiki article exposes `Continue in Think` only when its revision list contains a structurally accepted or preserved revision with a disposition receipt. Clicking it uses the existing exact investigation API and refuses unsafe returned routes.

## Automated evidence

Focused command:

```text
CI=true npm test -- --runInBand --watch=false \
  src/pages/ThinkMode.templates.test.jsx \
  src/components/wiki/WikiPageReadView.test.jsx \
  src/components/think/CalmIndexView.test.jsx \
  src/components/think/concepts/ConceptEvidenceStreamView.test.jsx \
  src/components/think/concepts/ConceptInvestigationPanel.core.test.jsx \
  src/layout/RightDrawer.test.jsx \
  src/styles/stitchEditorialCss.test.js
```

Result: **7 suites / 141 tests passed**.

`CI=true npm run build` passed. `git diff --check` passed.

The Think and Wiki suites retain existing React `act(...)` warnings around asynchronous ReferencePullIn and explicit maintenance-action hydration. The checks pass, but this is not clean-console evidence.

## Rendered browser evidence

Real authenticated routed UI on the isolated `qa_wiki_seed` account; no response interception or mocked API responses.

- Think Home at 1440, 1320, and 430 has no horizontal overflow.
- The universal command leads the active work before the motion queue.
- At 430 the Thought partner opens as one dialog and closes with Escape.
- An existing Concept renders with the draft as the dominant center plane at 1440 and 430, with no horizontal overflow.
- The mobile Concept exposes one explicit Thought partner trigger.
- New Concept creation focuses the name input and leaves partner scan unchecked.
- A separate 430 reduced-motion context reports `prefers-reduced-motion: reduce`, preserves the selected Concept, exposes the drawer trigger, and has no horizontal overflow.
- The pre-existing read-only QA corpus still has zero structurally accepted/preserved revision fixtures; the persisted acceptance below used isolated disposable data instead of changing that corpus.

Screenshots:

- `output/noeis-product-realignment-pass3-2026-08-10/think-home-1440.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-home-1320.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-home-430.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-concept-1440.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-concept-430.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-new-concept-430.png`
- `output/noeis-product-realignment-pass3-2026-08-10/think-concept-430-reduced-motion.png`

## Imagen comparison

The active thought now reads as the primary object, with the working agent available but subordinate. The mobile presentation protects the writing plane. The remaining visual seam is that New Concept is still a compact creation form over the current Concept rather than a completely independent unsaved writing canvas; it is usable and agent-optional, but not yet the most ambitious blank-canvas expression.

## Persisted continuation acceptance

Evidence: `output/noeis-product-realignment-pass3b-2026-08-10/`

Result: **PASS**.

- A structurally accepted Wiki revision exposed `Continue in Think`; the click issued one real `POST /api/wiki/pages/:id/investigation` with the exact page, revision, and claim identities.
- Think opened one exact Concept ObjectId. Identity churn, cache-cold reload, stale display-name routing, foreign ObjectIds, and mismatched ObjectIds all preserved or failed closed without duplicate investigation writes.
- The investigation hydrated after real 200 responses, retained the exact accepted relationship, and opened article/highlight evidence at its original Library identity.
- Sources and the Concept object remained unique. The real `FIND_TENSION` action produced reviewable workbench state that survived reload.
- One seeded change draft and one seeded agent suggestion were dismissed through their real routed controls. Both removals persisted independently.
- Full accepted page content, accepted revision content, claims, and `createdFrom` hashes were byte-equivalent before and after both dismissals; the accepted revision count remained one.
- The routed investigation remained usable without horizontal overflow at 1440, 1320, and 430. The prior rendered matrix supplies drawer keyboard/Escape and focus-return proof; this persisted run separately reconfirmed reduced-motion investigation hydration.
- Cleanup passed with zero collections remaining in the disposable database.

Binding:

- Worktree manifest SHA-256: `b0cd3235290534b14ee68fb35bed9f81aff6871265e5e82f2ea3ad68ce27b7f7`
- Scorecard SHA-256: `571be0e9f17fd1d3e876255be3b2c6bc0c4d3b3f4998afa5aac05f630d7c6097`
- Proof classes: persisted and rendered locally; exact-tree, merged, deployed, and production are false.

## Pass 3 closure

Pass 3 is complete locally. The next implementation slice is Pass 4: one universal reference and backlink flow across Library, Think, and Wiki.
